# Smithery configuration for gitinstall MCP server
# https://smithery.ai/docs/config

startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
  commandFunction: |-
    (config) => ({ command: "gitinstall-mcp", args: [] })
